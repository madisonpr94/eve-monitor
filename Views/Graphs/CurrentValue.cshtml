@model List<string>

<div class="col-md-4">
    <div style="border: 1px solid #dfdfdf; border-radius: 10px; margin: 0.5em; padding: 2em 1em; margin-top: 2em; margin-bottom: 10em" class="text-center">
        <h3>@Model[0]</h3>
        @Model[1]
    </div>
</div>